#include <stdio.h>

int main() {
    int kondisi = 0;

    if (!kondisi) { // Operator logika NOT (!)
        printf("Kondisi adalah salah (NOT).\n");
    } else {
        printf("Kondisi adalah benar (NOT).\n");
    }

    return 0;
}

